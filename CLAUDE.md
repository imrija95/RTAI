Project instructions for agents live in @AGENTS.md — read it.

Key rules (see AGENTS.md for the full text):
- `VIBE.md` holds the project's invariants; edit it ONLY with the user's explicit consent,
  and read it before any larger change / rabbit hole.
- Code, comments, README and docs are **English only** and contain **no personal information**
  (no "your"/"user's" framing; describe hardware generically, e.g. "a laptop-class GPU
  (e.g. RTX A2000 4GB)"; no personal names/emails).
- Prove-in-small before allocating the cluster. Commit/push only when asked.
