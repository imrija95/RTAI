"""RTAI — a persistent self-modifying (SRWM) language model.

At runtime the model modifies its own weight matrix (with a delta rule) and the changes
persist across sessions. See README.md and the project plan.
"""
